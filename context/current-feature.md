# Current Feature

<!-- No active feature. Run /feature load to start one. -->

## Status

Not Started

## Goals

<!-- Populated by /feature load -->

## Notes

<!-- Populated by /feature load -->

## History

<!-- Keep This Updated. Earliest To Latest -->

- **2026-08-10** — Initial Next.js 16 (App Router) project setup via `create-next-app`, with TypeScript and Tailwind CSS v4. Placeholder home page (`src/app/page.tsx`), no backend/database/tests configured yet.
- **2026-08-10** — Dashboard UI Phase 1 (Layout & Setup) completed on `feature/dashboard-phase-1`. ShadCN UI initialized; `/dashboard` route added with a full-width top bar (logo, centered search, "New Collection"/"New item" buttons), dark mode by default, and placeholder Sidebar/Main sections. Switched app font to Libre Franklin and dark background to an anthracite gray.
- **2026-08-10** — Dashboard UI Phase 2 (Sidebar & Navigation) completed on `feature/dashboard-phase-2`. Collapsible desktop sidebar (icon-rail toggle) plus an always-on mobile drawer; TYPES section links to `/items/[type]` with per-type item counts and a Pro badge on Files/Images (hidden when the user is Pro); COLLECTIONS section is collapsible with Favorites/Recent links, favorited collections (starred) shown separately from the rest, each with item counts and slight indentation. Fixed a flexbox `min-height: auto` bug so the sidebar always fills the viewport height with the user footer pinned to the bottom and only the nav scrolling.
- **2026-08-10** — Dashboard UI Phase 3 (Main Area) completed on `feature/dashboard-phase-3`. Main content area with a 4-card stats row (items, collections, favorite items, favorite collections), a "Recent Collections" grid (top 4 collections ranked by most recently updated item, since `Collection` has no timestamp field), a "Pinned Items" grid, and a "Recent Items" grid (last 10 items by `updatedAt`). Added a compact relative-time formatter (`lib/format.ts`) and reusable `ItemCard`/`CollectionCard`/`StatsRow` components; all server components (no client interactivity needed).
- **2026-08-10** — Prisma 7 + Neon PostgreSQL setup completed on `feature/prisma-neon-setup`. Added `prisma.config.ts` (Prisma 7 moves datasource URL config out of `schema.prisma` into this file) and `prisma/schema.prisma` with the `prisma-client` generator (replaces `prisma-client-js`, outputs to `src/generated/prisma`, gitignored, generated via `postinstall`). Schema covers NextAuth's `Account`/`Session`/`VerificationToken`/`User`, plus `ItemType`, `Item`, `Collection`, `ItemCollection` (join table), and `Tag`. Resolved the three open questions from `project-overview.md`: `Tag` is scoped per-user (`@@unique([userId, name])`, not global), `ItemType.color` stays required with no schema-level fallback, and `Collection.defaultTypeId` uses an explicit `"CollectionDefaultType"` relation name to avoid colliding with `Item.itemTypeId`. Client is instantiated via the Neon serverless driver adapter (`@prisma/adapter-neon` + `@neondatabase/serverless` + `ws`) in `src/lib/prisma.ts` — Prisma 7 requires an explicit driver adapter, there's no more implicit query engine connection. Ran `prisma migrate dev --name init` against the Neon dev branch; `npm run build` and `npm run lint` both pass. NextAuth itself (the `next-auth`/`@auth/prisma-adapter` packages, sign-in flows) is not wired up yet — only the DB models it needs are in place.
- **2026-08-10** — Seed Data completed on `feature/seed-data`. Added `prisma/seed.ts`, wired up via `migrations.seed` in `prisma.config.ts` (Prisma 7 config, not a `package.json#prisma.seed` key) and run with `npm run db:seed` (`prisma db seed`). Added an optional `password` field to `User` (bcrypt hash, null for OAuth-only accounts) via a new migration, since credentials auth isn't wired up yet but the spec needed somewhere to store the demo password hash — installed `bcryptjs` for hashing (12 rounds). Seeds the demo user (`demo@devstash.io` / `12345678`), the 7 system `ItemType`s, and 5 collections (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources) with their items per `context/features/seed-spec.md`. Script is idempotent: user/item-types use upsert-or-find, and collection/item seeding is skipped entirely if the demo user already has collections. `npm run build` and `npm run lint` both pass.
- **2026-08-10** — Dashboard Collections completed on `feature/dashboard-collections`. Added `src/lib/db/collections.ts` with `getRecentCollections()` and `getCollectionStats()`, querying Prisma directly (no auth wired up yet, so it looks up the demo user by email like the seed script does). `dashboard/page.tsx`'s "Recent Collections" section and the Collections/Favorite-collections stats now come from the Neon database instead of `lib/mock-data.ts`; items/pinned/recent-items sections stay on mock data since items aren't migrated yet. `CollectionCard` now derives its dot color from the most-used item type in the collection and shows small icons for every type present. Bumped the grid from 4 to 6 cards (3 cols × 2 rows) per the spec. Verified against the live dev server that real seeded collections render with correct counts/colors/icons. `npm run build` and `npm run lint` both pass.
- **2026-08-10** — Dashboard Items completed on `feature/dashboard-items`. Added `src/lib/db/items.ts` with `getPinnedItems()`, `getRecentItems()`, and `getItemStats()`, following the same demo-user-by-email pattern as `collections.ts`. `dashboard/page.tsx` now fetches pinned/recent items and item stats (total, favorites) from Prisma instead of `lib/mock-data.ts`; `ItemCard` was updated to accept the new `ItemWithType` shape (icon/color read from the item's `itemType` relation, tags from the `Tag` relation) instead of looking up `itemTypes` by id from mock data. `lib/format.ts`'s `formatRelativeTime` now accepts `Date | string` since Prisma returns `Date` objects. Pinned-items section still renders nothing when there are no pinned items. `lib/mock-data.ts` is left in place since `SidebarContent.tsx` still depends on it for per-type counts (out of scope for this spec). Verified against the live dev server that real seeded items (e.g. "Docker full prune", "Lucide icon library") render with correct icons, colors, tags, and relative timestamps. `npm run build` and `npm run lint` both pass.
- **2026-08-10** — Stats & Sidebar completed on `feature/stats-sidebar`. Sidebar (`SidebarContent.tsx`, `Sidebar.tsx`, `MobileSidebar.tsx`) now reads real database data instead of `lib/mock-data.ts`: item types come from a new `getItemTypesWithCounts()` in `src/lib/db/items.ts` (per-user item counts via `groupBy`, sorted by an explicit display order — Snippets, Prompts, Commands, Notes, Links, Files, Images — rather than DB insertion order), and collections come from two new functions in `src/lib/db/collections.ts` (`getFavoriteCollections()`, `getSidebarRecentCollections()`) built on a shared `getCollectionsWithStats()` helper refactored out of the existing `getRecentCollections()`. Added a "View all collections" link to `/collections` under the sidebar's recent-collections list. `dashboard/layout.tsx` fetches this data server-side and passes it down as props to the (client) sidebar components. The main-area stats row (`StatsRow`) was already fully DB-backed from prior features, so no changes were needed there. Verified via SSR HTML from the live dev server that the sidebar renders all 7 real item-type routes with correct counts/order and the 5 real seeded collections (real DB ids, not mock ones); `npm run build` and `npm run lint` both pass.
- **2026-08-10** — Add Pro Badge to Sidebar completed on `feature/add-pro-badge-sidebar`. Restyled the existing ShadCN `Badge` shown next to Files/Images in `SidebarContent.tsx` (rendered when `!currentUser.isPro`): switched from `variant="secondary"` to `variant="outline"` and added `uppercase tracking-wide text-muted-foreground` for a subtler, uppercase "PRO" look. No new components or logic — the conditional render and `proTypeSlugs` gating from the earlier Dashboard UI Phase 2 feature were already in place. Verified via SSR HTML with `currentUser.isPro` temporarily toggled to `false` that both Files and Images render the badge correctly; `npm run build` passes.
- **2026-08-13** — Fix N+1 / Redundant Dashboard Queries completed on `feature/fix-dashboard-n1-queries`. Surfaced by a `code-scanner` audit: `getDemoUserId()` was independently defined and DB-queried (`findUniqueOrThrow`) in every exported function of both `src/lib/db/collections.ts` and `src/lib/db/items.ts`, and `getCollectionsWithStats()` (full `collection.findMany` with nested `items → item → itemType`) was independently re-run by `getRecentCollections`, `getFavoriteCollections`, and `getSidebarRecentCollections` — together triggering 5 user lookups and 3 full collections queries per `/dashboard` request. Extracted `getDemoUserId()` into a new shared `src/lib/db/user.ts`, wrapped in React's `cache()` so it resolves once per request; wrapped `getCollectionsWithStats()` in `cache()` too so the three sidebar/dashboard views share one query instead of re-running it. Performance-only refactor — no behavior change (counts/ordering/favorites verified identical via SSR HTML) and no auth/session work (`demo@devstash.io` lookup pattern preserved as-is). `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Auth Setup (Phase 1) - NextAuth + GitHub Provider completed on `feature/auth-setup`. Installed `next-auth@5.0.0-beta.32` and `@auth/prisma-adapter`; split config into `src/auth.config.ts` (edge-compatible, GitHub provider only) and `src/auth.ts` (Prisma adapter, `session: { strategy: 'jwt' }`, `jwt`/`session` callbacks exposing `session.user.id`), following Auth.js's edge-compatibility guidance verified via Context7. Added `src/app/api/auth/[...nextauth]/route.ts` (destructures `GET`/`POST` from `handlers` — direct re-export doesn't work in v5 beta), `src/proxy.ts` (matcher-scoped to `/dashboard/:path*`, redirects unauthenticated users to `/api/auth/signin?callbackUrl=...`), and `src/types/next-auth.d.ts` (extends `Session.user` with `id`). Uses NextAuth's default sign-in page (no custom `pages.signIn`). Added `AUTH_SECRET` to `.env`/`.env.example` (`AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` were already present in `.env`). Verified end-to-end via Playwright against the live dev server: `/dashboard` redirects to sign-in, and "Sign in with GitHub" correctly kicks off the OAuth flow to github.com with the right client ID and callback URL; didn't complete a real GitHub login (out of scope). Full sign-in/callback round-trip and credentials/email auth are not covered — GitHub OAuth only. `npm run build` passes.
- **2026-08-14** — Auth Credentials (Phase 2) - Email/Password Provider completed on `feature/auth-credentials`. Added a Credentials provider alongside GitHub OAuth: `src/auth.config.ts` carries an edge-compatible placeholder (`authorize: () => null`), overridden in `src/auth.ts` with real bcrypt-based validation against `User.password` (field already existed from the earlier seed feature — no new migration needed). Added `POST /api/auth/register` (`src/app/api/auth/register/route.ts`), validating `name`/`email`/`password`/`confirmPassword` with `zod` (newly installed — coding-standards.md requires Zod for input validation), checking for an existing user, and hashing with `bcryptjs` (12 rounds, matching the seed script). Verified via curl (success, duplicate-email, and mismatched-password cases) and via Playwright against the live dev server: credentials sign-in from `/api/auth/signin` reaches `/dashboard`, and GitHub OAuth still correctly redirects to github.com with the right client ID/callback URL afterward. `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Auth UI (Phase 3) - Sign In, Register & Sign Out completed on `feature/auth-ui`. Added custom `/sign-in` (`src/app/sign-in/page.tsx` + `SignInForm.tsx`: email/password + "Sign in with GitHub", error display, respects `callbackUrl`) and `/register` (`src/app/register/page.tsx` + `RegisterForm.tsx`: client-side Zod validation mirroring the Phase 2 API schema, posts to `POST /api/auth/register`, redirects to sign-in on success with a sonner success toast). `src/auth.config.ts` now sets `pages.signIn: "/sign-in"`; `src/proxy.ts` redirects unauthenticated users to `/sign-in` (was `/api/auth/signin`) and now also gates `/profile`. Added a new minimal `/profile` page and a reusable `UserAvatar` component (`src/components/ui/user-avatar.tsx`, GitHub image or name-initials fallback). Sidebar footer (`SidebarContent.tsx`/`Sidebar.tsx`/`MobileSidebar.tsx`) now receives the real NextAuth session (fetched once in `dashboard/layout.tsx`) instead of `lib/mock-data.ts`'s `currentUser` for the avatar/name/email; the avatar links to `/profile` while clicking the name/email opens a shadcn dropdown-menu (added via `npx shadcn add dropdown-menu label`) with a "Sign out" item calling `next-auth/react`'s `signOut`. Added shadcn's `sonner` Toaster (`src/components/ui/sonner.tsx`, mounted once in root `layout.tsx`) with `richColors` enabled so success/error toasts are tinted correctly; dropped the unused `next-themes` dependency `sonner`'s CLI install pulled in since this app is dark-mode-only. Verified end-to-end via Playwright against the live dev server: custom sign-in page renders, credentials sign-in reaches `/dashboard`, GitHub OAuth button correctly redirects to github.com, sidebar avatar/name render from the real session, avatar click navigates to `/profile`, dropdown sign-out clears the session and redirects to `/sign-in` (re-confirmed `/dashboard` is protected again afterward), and registering a new account shows the green success toast and redirects to `/sign-in`, with the new account then able to sign in. `npm run build` and `npm run lint` both pass. A pre-existing, unrelated local edit to `next.config.ts` (devIndicators, quote style) was intentionally left uncommitted rather than bundled into this feature.
- **2026-08-14** — Email Verification on Register completed on `feature/email-verification`. New credentials registrations now go through `createVerificationToken`/`sendVerificationEmail` (`src/lib/verification-email.ts`), reusing the existing `VerificationToken` model (identifier/token/expires, 24h TTL, single-use — deleted on consumption or on a fresh resend) rather than adding a new one, and the existing `User.emailVerified` column for the gate. `POST /api/auth/register` sends the email via a new `src/lib/resend.ts` client right after user creation (best-effort — failures are logged, not thrown, so registration still succeeds). Added `GET /api/auth/verify-email?token=...` (validates + consumes the token, sets `emailVerified`, redirects to `/sign-in?verified=1` or `?verified=0`) and `POST /api/auth/resend-verification` (always returns generic success so it can't be used to enumerate accounts). `src/auth.ts`'s Credentials `authorize()` now throws a custom `EmailNotVerifiedError extends CredentialsSignin` (`code: "email_not_verified"`) when unverified, surfaced to the client via `next-auth/react`'s typed `SignInResponse.code`; a new `signIn` callback auto-marks GitHub OAuth users verified. `SignInForm.tsx` shows a "Resend verification email" action on that specific error and toasts on the `verified` query param. Also added `scripts/delete-non-demo-users.ts` (`npm run db:clean`, dry-run by default, `--force` to actually delete) for clearing out test accounts created during manual testing. Verified end-to-end via curl against the live dev server: registration → blocked sign-in with `code=email_not_verified` → verify link flips `emailVerified` and redirects with `verified=1` → reusing the same link now fails (`verified=0`, single-use confirmed) → sign-in then succeeds with a real session cookie → resend-verification returns identical responses for existing/nonexistent accounts. Hit a real blocker during testing: the intended `from` domain isn't verified in Resend (403 on send), so per user's choice the `from` address is temporarily `onboarding@resend.dev` (a `TODO` in `verification-email.ts` flags the domain-verified swap-back) — Resend's shared sender only delivers to the Resend account owner's own address until a domain is verified, so real end-user delivery is blocked until that domain step is done outside this session. `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Email Verification Toggle + Silent-Failure Fix completed on `feature/email-verification-toggle`. Added `EMAIL_VERIFICATION_ENABLED` (`src/lib/verification-email.ts`'s `isEmailVerificationEnabled()`, defaults to enabled unless explicitly `"false"`) so verification can be turned off entirely for environments without a verified Resend sending domain — with it off, `POST /api/auth/register` creates users already `emailVerified` and skips the Resend call altogether, and `authorize()`/`resend-verification` both short-circuit around the check. While wiring this up, found and fixed a real bug in the original feature: `resend.emails.send()` resolves with `{ data, error }` rather than throwing on API failures, so the original `try/catch` around it could never actually catch a failed send — registration always reported success even when no email went out. `POST /api/auth/register` now checks that `error` field and throws, and with the flag on, a failed send rolls back the just-created user and returns a real `502` instead of a silent success. Verified end-to-end against the live dev server with the flag both off (auto-verified user, sign-in succeeds immediately, no Resend call attempted) and on (a send guaranteed to fail via Resend's sandbox restriction now correctly returns `success: false` and leaves no orphaned user; a send to the Resend account's own address still succeeds as before). `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Forgot Password completed on `feature/forgot-password`. Added `src/lib/password-reset.ts` (`createPasswordResetToken`/`sendPasswordResetEmail`/`consumePasswordResetToken`), reusing the existing `VerificationToken` model but with a `password-reset:${email}`-namespaced identifier so a reset request can't collide with or wipe a pending email-verification token for the same address (`createVerificationToken` in `verification-email.ts` deletes by bare-email identifier). Same 24h TTL and single-use-delete-on-consumption pattern as email verification. Added `POST /api/auth/forgot-password` (generic success always, matching the `resend-verification` anti-enumeration pattern — only actually sends when the account has a password set) and `POST /api/auth/reset-password` (validates/consumes the token, bcrypt-hashes at 12 rounds, rejects OAuth-only accounts with no password with a clear error). Added `/forgot-password` and `/reset-password` pages plus `ForgotPasswordForm`/`ResetPasswordForm` client components, matching `SignInForm`/`RegisterForm` conventions (Zod, sonner, shadcn `Card`); added a "Forgot password?" link next to the password field in `SignInForm.tsx`. Caught and fixed a UI bug during review: the `/forgot-password` page's static instruction text ("Enter your email...") lived in the server-rendered `CardDescription` while the form's own submitted-state message rendered separately, so both showed at once after submitting — moved the instruction text into `ForgotPasswordForm` itself so it toggles off correctly once submitted (verified via Playwright). Verified end-to-end against the live dev server via curl + a direct DB read of the created token: request → token created → mismatched-password rejected → invalid token rejected → valid reset succeeds → reused token now fails (single-use confirmed) → signed in successfully with the new password (real session), then restored the demo user's original password. `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Profile Page completed on `feature/profile-page`. Added `src/lib/db/profile.ts` (`getProfileStats(userId)`: total items, total collections, per-type breakdown across all 7 system types) and `src/lib/db/user.ts`'s new `getUserProfile(userId)` (name/email/image/join date/`hasPassword`), both scoped to the real signed-in user via `session.user.id` rather than the dashboard's demo-user hack. Rebuilt `/profile` (`src/app/(app)/profile/page.tsx`) on top of the minimal page from `feature/auth-ui`: account-info card, usage-stats card, and an account-actions card with `ChangePasswordDialog` (hidden for GitHub-only accounts with no password) and `DeleteAccountDialog`. Added `POST /api/auth/change-password` (verifies current password, bcrypt-hashes new one) and `POST /api/auth/delete-account` (deletes the user; existing `onDelete: Cascade` on `Item`/`Collection`/`Tag`/etc. handles cleanup). Installed shadcn `dialog`/`alert-dialog`. Mid-feature, moved `/dashboard` and `/profile` under a shared `src/app/(app)/layout.tsx` route group (URLs unchanged) so profile renders inside the sidebar/top-bar shell instead of full-screen — per user feedback that the standalone page took up the whole screen. Also hardened delete-account per user feedback: the confirmation dialog now requires typing `DELETE` (case-sensitive) before the destructive button enables, instead of a plain confirm/cancel. Verified end-to-end via Playwright against the live dev server: real stats render (18 items / 5 collections, per-type counts), change-password rejects a wrong current password with an inline error and succeeds on a correct one (round-tripped back to the original demo password afterward), and delete-account's typed-confirmation gating works (wrong/lowercase text keeps the button disabled, exact `DELETE` enables it; cancelled without deleting). A review pass caught and fixed one pattern deviation before completion: the page was calling `prisma.user.findUniqueOrThrow` directly instead of going through the `src/lib/db` layer like every other page component. `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Auth Security Audit + Enumeration Fix completed on `fix/register-enumeration-leak`. Added a new `auth-auditor` subagent (`.claude/agents/auth-auditor.md`, Glob/Grep/Read/Write, sonnet) scoped to auditing NextAuth-adjacent code NextAuth doesn't handle automatically (password hashing, rate limiting, token security), explicitly excluding NextAuth-managed concerns (CSRF, cookie flags, OAuth state). First run audited `auth.ts`/`auth.config.ts`/`proxy.ts`, all `/api/auth/*` routes, `verification-email.ts`, `password-reset.ts`, and the profile page/actions, writing results to `docs/audit-results/AUTH_SECURITY_REVIEW.md`. Findings: no rate limiting anywhere (sign-in, register, forgot-password, resend-verification, change-password all accept unlimited attempts — flagged High, not yet fixed) and `POST /api/auth/register` leaked account existence via a `409` on duplicate email, inconsistent with the anti-enumeration pattern already used by `forgot-password`/`resend-verification` (flagged Medium). Fixed the enumeration leak: duplicate-email registration now returns the identical `{ success: true }` response as a real registration instead of a `409`; `RegisterForm.tsx` needed no changes since it only branches on `body.success`. Everything else audited passed (bcrypt 12 rounds throughout, 256-bit random tokens with 24h TTL and single-use enforcement, namespaced reset vs. verification tokens, OAuth-only accounts correctly rejected from password flows, profile page/mutations correctly session-scoped). Rate limiting remains an open follow-up, not addressed in this fix. `npm run build` passes. Merged to `main` via PR #1, both local and remote `fix/register-enumeration-leak` branches deleted.
- **2026-08-14** — Dashboard Real-User Migration + Housekeeping completed on `fix/dashboard-real-user`. Surfaced by a `/cleanup` pass: `src/lib/db/user.ts`'s `getDemoUserId()` (hardcoded `demo@devstash.io` lookup) was still backing every dashboard/sidebar query even though NextAuth has been fully wired up since `feature/auth-setup`, and `SidebarContent.tsx`'s Pro badge still read `isPro` from the stub `lib/mock-data.ts` instead of the real signed-in user. Removed `getDemoUserId()`; `getItemTypesWithCounts`, `getFavoriteCollections`, `getSidebarRecentCollections`, `getRecentCollections`, `getCollectionStats`, `getPinnedItems`, `getRecentItems`, and `getItemStats` now all take a real `userId` param sourced from `auth()`'s session in `(app)/layout.tsx` and `(app)/dashboard/page.tsx`, matching the pattern `getUserProfile`/`getProfileStats` already used. Extended the NextAuth session to carry a real `isPro` field (`src/auth.ts`'s `jwt`/`session` callbacks, `Session` type augmentation) so `SidebarContent.tsx` can read `user.isPro` instead of mock data; deleted `src/lib/mock-data.ts` (no other consumers). Hit one real snag: augmenting `next-auth/jwt`'s `JWT` interface doesn't merge (it's only re-exported from `@auth/core/jwt`, not declared in that module), causing a `Type '{}' is not assignable to type 'boolean'` build error — worked around it with a runtime `typeof` check when reading `token.isPro` in the `session` callback instead of relying on the ineffective type augmentation. Also added `NEXT_PUBLIC_APP_URL` to `.env.production` (was missing vs. `.env`/`.env.example`, another cleanup finding). Verified via Playwright against the live dev server: signed in as the demo user, dashboard renders real sidebar counts/collections/stats from the database, Files/Images "Pro" badges correctly reflect the real (false) session `isPro`, no console errors. `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Code-Scanner Findings Fix completed on `fix/code-scanner-findings`. Fixed all 10 findings (2 critical, 4 warning, 4 suggestion) from a `code-scanner` agent run: `getUserProfile` now uses `findUnique` + redirects to sign-in on a missing user instead of throwing via `findUniqueOrThrow`; added a `passwordChangedAt` column (new migration) embedded in the JWT at sign-in, with `src/auth.ts`'s `jwt` callback invalidating the token if the DB value has since moved past it (`change-password`/`reset-password` both bump it) — this is the "invalidate other sessions on password change" fix. Added `try/catch` to all 6 auth/profile client forms (`RegisterForm`, `SignInForm`, `ForgotPasswordForm`, `ResetPasswordForm`, `ChangePasswordDialog`, `DeleteAccountDialog`) so a network failure resets loading state and shows an error instead of leaving the button stuck; `verify-email` falls back to `req.nextUrl.origin` if `NEXT_PUBLIC_APP_URL` is unset. Extracted `src/lib/db/item-types.ts` to dedupe the `ITEM_TYPE_DISPLAY_ORDER`/formatting logic shared by `items.ts` and `profile.ts`, and replaced the naive `+ "s"` slug pluralization with a small rule-based `pluralize()`. Suggestions: `aria-label="Search"` on the top-bar search input, GitHub avatars now render through `next/image` (via Base UI's `render` prop, `remotePatterns` configured in `next.config.ts`), `aria-hidden="true"` on decorative color dots, `next.config.ts` reformatted to 2-space indent. Two real bugs surfaced and fixed mid-implementation, not part of the original scan: the JWT-invalidation fix crashed `(app)/layout.tsx` and `dashboard/page.tsx` with a 500 (`session!.user` non-null assertion, since `proxy.ts`'s edge-only auth check can't see the DB-based invalidation) — both now redirect to `/sign-in` gracefully instead of asserting non-null; and the invalidation check's DB lookup ran on every `auth()` call, which layout/page/profile each call independently per request — wrapped the exported `auth` in React's `cache()` (same pattern as `getDemoUserId()`/`getCollectionsWithStats()`) to dedupe it, found during the `/feature review` pass. Verified end-to-end via Playwright against the live dev server: full change-password → forced re-login → sign-in-with-new-password → restore-original-password round trip, plus dashboard/sidebar/profile all rendering correctly with no console errors. `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Code-Scanner Re-Scan Fixes completed on `fix/code-scanner-rescan`. A follow-up `code-scanner` re-scan of the previous feature's merge found no critical issues but two warnings, both fixed here: `sendVerificationEmail`/`sendPasswordResetEmail` (`src/lib/verification-email.ts`, `src/lib/password-reset.ts`) now take a request-derived `origin` parameter and fall back to it (`process.env.NEXT_PUBLIC_APP_URL ?? origin`) instead of relying solely on the env var, matching the pattern already applied to `verify-email/route.ts` in the last fix — all three call sites (`register`, `resend-verification`, `forgot-password` routes) updated to pass `new URL(request.url).origin`; and `src/lib/db/profile.ts`'s `getProfileStats` now calls the shared `getSystemItemTypesOrdered()` helper instead of reimplementing the item-type fetch+sort inline, finishing the dedup the previous feature's `item-types.ts` extraction was meant to achieve. Three re-scan suggestions (placeholder `layout.tsx` metadata, `pluralize()`/slug fragility for future custom types, inert `TopBar` search input) were left out of scope. Verified via Playwright (profile page renders identically with correct type-breakdown ordering) and curl against the live dev server (`forgot-password` reaches Resend successfully with the new signature, failing only on the known sandbox `from`-address restriction, not on a bad URL). `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Rate Limiting for Auth completed on `feature/rate-limiting-auth`, closing the "no rate limiting anywhere" High finding from the earlier `fix/register-enumeration-leak` security audit. Added `src/lib/rate-limit.ts`: an Upstash Redis (`@upstash/ratelimit` + `@upstash/redis`) sliding-window utility (`checkRateLimit`, `getClientIp`, `rateLimitResponse`) that fails open (allows the request) if Upstash is unconfigured or unreachable. Five named limiters match the spec exactly — login (5/15min, IP+email), register (3/1hr, IP), forgot-password (3/1hr, IP), reset-password (5/15min, IP), resend-verification (3/15min, IP+email). The four plain API routes (`register`, `forgot-password`, `reset-password`, `resend-verification`) return a 429 with `{ success: false, error }` and a `Retry-After` header. Login is limited inside the Credentials provider's `authorize()` in `src/auth.ts` rather than the NextAuth callback route itself (NextAuth owns that route, so it can't be wrapped directly) via a new `RateLimitedError extends CredentialsSignin` (`code: "rate_limited"`), matching the spec's own note that login would need a custom handler. Frontend error display followed each component's existing convention rather than forcing toast everywhere: `RegisterForm`/`ResetPasswordForm` already displayed `body.error` inline and needed no changes; `SignInForm` now branches on `code === "rate_limited"`; `handleResendVerification` and `ForgotPasswordForm` previously ignored the response body entirely and now check `body.success`. Verified end-to-end against the live dev server using the real Upstash Redis credentials already present in `.env` (not just the fail-open path): register allowed 3 requests then 429'd on the 4th with a correct `Retry-After`, and credentials login allowed 5 failed attempts then returned `code=rate_limited` on the 6th; test users and the Redis keys created during verification were cleaned up afterward. `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Change Password Rate Limiting completed on `fix/change-password-rate-limit`. A re-run of the `auth-auditor` agent immediately after `feature/rate-limiting-auth` merged found one gap the feature missed: `POST /api/auth/change-password` had no rate limiting on its `currentPassword` check, letting a hijacked session be used as an unlimited password-guessing oracle (Medium severity — no extra DevStash access, but leaks the user's real plaintext password for reuse elsewhere). Added a sixth named limiter, `changePassword` (5/15min, matching `resetPassword`'s cadence), to the existing `src/lib/rate-limit.ts` — no new infra needed — keyed by `session.user.id` rather than IP since the endpoint is already session-gated; wired into `change-password/route.ts` via the same `checkRateLimit`/`rateLimitResponse` pattern as the other five endpoints, checked before the `bcrypt.compare` call. `ChangePasswordDialog.tsx` needed no changes since it already displayed `body.error` on any `!body.success` response. Updated `docs/audit-results/AUTH_SECURITY_REVIEW.md` to move the finding from Medium into Passed Checks. Verified end-to-end against the live dev server signed in as the demo user: 5 wrong-current-password attempts returned the normal 400, the 6th correctly returned 429 with `Retry-After`; demo password was untouched (deliberately wrong current password used throughout) and the test Redis key was cleaned up. `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Sign-In Server Actions completed on `feature/signin-server-actions`. Converted `SignInForm.tsx`'s credentials and GitHub sign-in from client-side `next-auth/react` `signIn()` calls to NextAuth v5 Server Actions, per Auth.js's documented App Router pattern (the `next-auth/react` client version's own reference docs say "For Server Actions, use the `signIn` method imported from the `auth` config"). Added `src/actions/auth.ts` (`signInWithCredentials`, `signInWithGithub`, both calling `signIn` from `@/auth`), consumed via two `useActionState` hooks in `SignInForm.tsx` instead of manual `useState`/`onSubmit` handling; GitHub's callback URL is passed via `signInWithGithub.bind(null, callbackUrl)` since `useActionState` actions must be `(prevState, formData) => state`. Both actions catch `CredentialsSignin`/`AuthError` and return `.code`/`.type` instead of letting NextAuth redirect to an error page — `RateLimitedError`/`EmailNotVerifiedError` (unchanged in `src/auth.ts`, zero diff) still surface their custom codes through this path exactly as before. All existing UX preserved: inline error text, "Resend verification email" button on `email_not_verified`, per-form pending state via `useActionState`'s third return value, `callbackUrl` respected (hidden input for credentials, bound arg for GitHub). Verified end-to-end via Playwright against the live dev server: wrong password → inline "Invalid email or password"; 6 varied-password attempts → 6th correctly shows "Too many attempts..." (`rate_limited`); correct password → redirects to `/dashboard` with a real session; "Sign in with GitHub" → correctly redirects to github.com with the right client ID/callback URL. Hit a significant debugging detour confirming the rate-limit path stayed correct through the refactor: `@upstash/ratelimit`'s `Ratelimit` instances default to an in-memory *ephemeral cache* (`node_modules/@upstash/ratelimit/dist/index.mjs`, `config.ephemeralCache === void 0` branch) that, once an identifier is blocked, short-circuits future `.limit()` calls from memory without touching Redis again until the block window passes — so deleting Redis keys between manual tests did NOT reset rate-limit state within the same running dev server process, only a full server restart does. This is intentional, documented library behavior (not a bug), but is an important gotcha for anyone manually testing rate limits in this dev environment: **restart the dev server, not just clear Redis, to get a clean slate.** `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Delete Account Server Action completed on `feature/delete-account-server-action`. A follow-up to `feature/signin-server-actions`: reviewed all remaining `/api/auth/*` routes to see which were also good Server Action candidates, and found that `register`/`forgot-password`/`reset-password`/`resend-verification`/`change-password` deliberately stay as API routes since they rely on `429` + `Retry-After` headers from the rate-limiting feature — something Server Actions can't cleanly express, and an explicit reason `coding-standards.md` itself lists for preferring API routes ("Specific HTTP status codes or headers"). `delete-account` had no such constraint (no rate limiting, just `{ success, error }` + a `401`), making it the one clean candidate. Added `deleteAccount()` to `src/actions/auth.ts` — reads `session.user.id` via `auth()`, deletes the user via Prisma, then calls `signOut({ redirectTo: "/" })` (which redirects internally, same non-returning pattern as `signIn`; needed an explicit unreachable `return { success: true }` after it to satisfy TypeScript, since it couldn't narrow the call's return type to `never` from context). Deleted `src/app/api/auth/delete-account/route.ts` (confirmed gone from the build's route list) and updated `DeleteAccountDialog.tsx` to call the action directly instead of `fetch` + `next-auth/react`'s `signOut`; typed-`DELETE`-confirmation gating and disabled-while-deleting state were untouched. Verified end-to-end via Playwright against a disposable test account (registered via `POST /api/auth/register`, never the demo user, given this is destructive): typed confirmation correctly gated the button, clicking delete redirected straight to `/`, `/dashboard` afterward bounced back to `/sign-in` (session cleared), and `npm run db:clean`'s dry-run listing confirmed the test account was actually gone from the database. `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Items List View completed on `feature/items-list-view`. Added the dynamic route `src/app/(app)/items/[type]/page.tsx`, listing a signed-in user's items filtered by type in a responsive grid matching the dashboard's card layout (`sm:grid-cols-2 lg:grid-cols-3`, slightly wider `gap-4`). Added `getItemTypeBySlug(slug)` (`src/lib/db/item-types.ts`) to resolve the route's `[type]` slug to a real `ItemType` via the existing `pluralize()`, calling Next.js's `notFound()` for an unknown slug, and `getItemsByType(userId, itemTypeId)` (`src/lib/db/items.ts`) to fetch the matching items — both reusing the existing `ItemWithType`/`ItemCard` shapes as-is, including `ItemCard`'s colored-left-border-by-type styling, with no changes needed there. Page follows the same auth-guard pattern as `dashboard/page.tsx` and uses Next.js 16's generated `PageProps<"/items/[type]">` typing per `CLAUDE.md`'s guidance. A `/feature review` pass caught that `getSystemItemTypesOrdered()` was now running twice per request (once from the sidebar's `getItemTypesWithCounts()` in `(app)/layout.tsx`, once from the new page's `getItemTypeBySlug()`) — fixed by wrapping it in React's `cache()`, matching the dedup pattern `collections.ts`'s `getCollectionsWithStats` already established. Verified via Playwright against the live dev server: sidebar links navigate correctly into `/items/snippets`, `/items/links`, `/items/commands` with correct counts/active-state highlighting, colored borders per type, uniform card heights regardless of description length, 3-column grid at desktop width collapsing to 1 column on mobile, an empty type (`/items/notes`) shows "No notes yet.", and an invalid slug (`/items/bogus`) correctly 404s. `npm run build` and `npm run lint` both pass.
- **2026-08-14** — Vitest Unit Testing Setup completed on `feature/vitest-setup`. Added `vitest.config.mts` (`.mts` extension avoids a CJS/ESM warning without needing `"type": "module"` in `package.json`; `resolve.tsconfigPaths: true` handles `@/` aliases natively — no `vite-tsconfig-paths` dependency needed), `node` test environment, `include` scoped to `src/actions/**/*.test.ts` and `src/lib/**/*.test.ts` only (deliberately excludes `.tsx`/components — no jsdom/Testing Library configured). Added `npm test` (`vitest run`) and `npm run test:watch` (`vitest`) scripts. Wrote example tests: `src/lib/format.test.ts` (`formatRelativeTime`, pure), `src/lib/db/item-types.test.ts` (`pluralize`/`formatItemTypeName`, mocking `@/lib/prisma` since the module also exports a DB-backed function), and `src/actions/auth.test.ts` (`deleteAccount`, mocking `@/auth`/`@/lib/prisma`). Hit a real blocker writing the server-action test: importing anything from the real `next-auth` package inside Vitest fails (`Cannot find module 'next/server'`) because `next-auth`'s `lib/env.js` eagerly imports `next/server`, and Next.js's `package.json#exports` map is empty (Next relies on its own bundler's non-standard resolution, which strict-resolution tools like Vite/Vitest can't use) — fixed by also mocking the `next-auth` package itself, re-exporting the real `AuthError`/`CredentialsSignin` classes from `@auth/core/errors` (framework-agnostic, doesn't pull in `next/server`) so `instanceof` checks in `src/actions/auth.ts` still work correctly. `src/lib/db/*` (Prisma/Neon data-access) stays out of scope — would need integration-style DB tests, not unit tests. Documented the scope and the `next-auth` mocking gotcha in a new "Unit Testing" section in `context/ai-interaction.md` (also updated the workflow's **Test** step, which previously said "Implement unit testing later"), added a one-line test-file convention to `coding-standards.md`'s File Organization section, and updated `CLAUDE.md`'s stale "no test suite" claim and Commands list. `npm test`, `npm run build`, and `npm run lint` all pass (12 tests, 3 files).
- **2026-08-14** — Item Drawer completed on `feature/item-drawer`. Right-side shadcn `Sheet` opens on `ItemCard` click across both the dashboard and `/items/[type]` pages, showing full item detail fetched on demand via a new `GET /api/items/[id]` route (auth-checked, backed by a new `getItemDetail(userId, itemId)` in `src/lib/db/items.ts` that scopes ownership via `findFirst({ id, userId })` to prevent IDOR). `ItemCard` became a client component (mouse + keyboard activation) driven by a new `item-drawer-context.tsx` (`ItemDrawerProvider`/`useItemDrawer`), mirroring the existing `sidebar-context.tsx` pattern, mounted once in `(app)/layout.tsx` alongside the drawer itself. Drawer body shows labeled sections (Description, Content/URL/File depending on `contentType`, Tags, Collections with a folder icon and badge-style entries, and a Details block with Created/Updated/Language) per user feedback through several follow-up passes on spacing and label casing; action bar has Favorite/Pin/Copy/Edit/Delete matching the spec screenshot, with only Copy actually wired up (clipboard + toast) — Favorite/Pin/Edit/Delete render correct visual state but are inert, intentionally deferred per the spec's "details display only" scope. Loading state is derived (`!item || item.id !== openItemId`) rather than tracked via `setState` directly in a `useEffect` body, to satisfy this repo's `react-hooks/set-state-in-effect` lint rule — caught and fixed a real bug during the `/feature review` pass where an earlier "simplification" of that derived condition would have crashed on first page load (before any item was ever opened) by breaking TypeScript's null-narrowing through the ternary; fixed by inlining the check so `item` narrows correctly in the JSX branch. No new unit tests: `getItemDetail` is a `src/lib/db/**` Prisma query wrapper, explicitly out of the unit-testing scope documented in `context/ai-interaction.md` (same as every other function in that file); the API route and client components are likewise outside the configured Vitest scope. Verified end-to-end via Playwright against the live dev server across both pages and multiple item types (command, link), including console-error checks on initial load. `npm run build`, `npm run lint`, and `npm test` (12 tests, unchanged) all pass.
- **2026-08-14** — Item Drawer Edit Mode completed on `feature/item-drawer-edit-mode`. Clicking the drawer's Edit button now swaps the action bar for Save/Cancel and turns Title/Description/Tags (all types) plus Content (snippet/prompt/command/note)/Language (snippet/command)/URL (link) into controlled inputs, while item type/Collections/Created/Updated stay visible but non-editable. Added `updateItem(itemId, data)` in `src/actions/items.ts` (Zod-validated — non-empty trimmed title, optional nullable description/content/url/language, `url` checked as a real URL only when present, tags as a trimmed non-empty array — `{ success, data, error }` pattern, session via `auth()`) and a matching `updateItem` query fn in `src/lib/db/items.ts` (ownership-checked via `findFirst` before updating, tags handled as disconnect-all (`set: []`) then `connectOrCreate` against the per-user-unique `Tag` compound key, returns the updated `ItemDetail`); extracted a shared `toItemDetail` mapper so `getItemDetail` and the new `updateItem` don't duplicate the Prisma-shape-to-`ItemDetail` transform. `ItemDrawer.tsx`'s edit state resets on drawer close (via `Sheet`'s `onOpenChange`, a plain event handler — not an effect, so it doesn't trip the `set-state-in-effect` lint rule already worked around in the base drawer feature) rather than needing a second effect. Installed shadcn's `textarea` component (Description/Content fields). Verified end-to-end via Playwright against the live dev server: edited a real seeded command item's title/tags and confirmed the save round-tripped (toast, view-mode refresh, and the background card list updating live via `router.refresh()`), confirmed Cancel discards an in-progress edit (reverted to the last *saved* state, not the original), confirmed Save is disabled with an empty title, confirmed a link-type item's edit form shows only the URL field (no Content/Language) — then reverted the test edits back to the original seed data afterward. Added `src/actions/items.test.ts` (5 tests: no-session, empty-title validation, invalid-URL validation, item-not-found, happy-path delegation) mocking `@/auth`/`@/lib/db/items`, following the same pattern as `auth.test.ts` but without needing to mock the `next-auth` package itself since this action never imports it directly; the new `updateItem` query fn in `lib/db/items.ts` stays untested per the established `src/lib/db/**` exclusion. `npm run build`, `npm run lint`, and `npm test` (17 tests, up from 12) all pass.
- **2026-08-14** — Item Delete completed on `feature/delete-item`. Wired up the Item Drawer's previously-inert Delete button: wrapped it in a shadcn `AlertDialog` confirmation (plain confirm/cancel, matching `DeleteAccountDialog`'s pattern but without its typed-`DELETE`-confirmation gating, since account deletion is higher-stakes than item deletion). Added `deleteItem(itemId)` in `src/actions/items.ts` (`{ success, error }` pattern, session via `auth()`) and a matching `deleteItem(userId, itemId)` query fn in `src/lib/db/items.ts` (ownership-checked via `findFirst` before deleting, same shape as the existing `updateItem`). On confirm: success shows a sonner toast, closes the drawer, and calls `router.refresh()` so the background item grid and sidebar per-type counts update live (matching the edit-mode save flow); failure shows an error toast and leaves the drawer/state untouched. Added 3 tests to `src/actions/items.test.ts` (no-session, item-not-found, happy-path delegation), following the same pattern as the `updateItem` suite; the new `deleteItem` query fn in `lib/db/items.ts` stays untested per the established `src/lib/db/**` exclusion. Verified end-to-end via Playwright against the live dev server using a disposable test item (created directly via Prisma since there's no item-creation UI yet): confirmation dialog renders correctly, Cancel leaves the item untouched, confirming Delete removes it, shows the "Item deleted" toast, closes the drawer, and both the `/items/[type]` grid and sidebar count update immediately with no console errors — since dashboard and `/items/[type]` share the same `ItemDrawer` component, this single pass covers both surfaces. `npm run build`, `npm run lint`, and `npm test` (20 tests, up from 17) all pass.
- **2026-08-14** — Item Create completed on `feature/item-create`. Added a `NewItemDialog.tsx` (shadcn `Dialog`) wired to the top bar's "New item" button, with a type selector (Snippet/Prompt/Command/Note/Link, icons/colors sourced from the real `ItemType` records rather than hardcoded) and fields that adapt per type — Content+Language for snippet/command, Content for prompt/note, a required URL for link, Title/Description/Tags for all. Added `createItem(itemId, data)` → actually `createItem(input)` in `src/actions/items.ts` (Zod-validated: title required, URL required+validated only for `link`, `itemType` constrained to a 5-value enum so file/image — Pro-gated, out of this spec — can't be created this way) and a matching `createItem(userId, itemTypeId, data)` query fn in `src/lib/db/items.ts`. Added `getItemTypeByName(name)` to `src/lib/db/item-types.ts` to resolve the submitted type name to a real `ItemType` row, and exposed the raw singular type name as `ItemTypeWithCount.value` (previously only the formatted plural `name` was available) so the client can round-trip it through the enum-validated action. `TopBar` now receives `itemTypes` as a prop from `(app)/layout.tsx` (same data already fetched for the sidebar, filtered to the 5 non-Pro types) instead of being a fully static component. On success: sonner toast, dialog closes/resets, `router.refresh()` updates stats/sidebar counts/recent-items live. Verified end-to-end via Playwright against the live dev server: created a real snippet (all fields round-tripped correctly in the drawer afterward) and a real link (URL validation correctly blocked `not-a-url` with an inline error before allowing a valid URL through), both disposable test items deleted afterward to restore baseline counts. Also had to kill a stray leftover Playwright automation Chrome process (dedicated `mcp-chrome-*` profile, not the user's browser) that was blocking a new browser session. Added 6 tests to `src/actions/items.test.ts` covering the new action's validation branches and both the text and link `contentType` mappings. `npm run build`, `npm run lint`, and `npm test` (26 tests, up from 20) all pass.
- **2026-08-16** — Code Editor completed on `feature/code-editor`. Added `CodeEditor.tsx` (`src/components/dashboard/`), a Monaco-based editor (`@monaco-editor/react`, dynamically imported with `ssr: false` since Monaco needs `window`) with a macOS-style header (red/yellow/green window dots, uppercase language label, a copy-to-clipboard icon button with sonner toast), `vs-dark` theme, and a fluid height driven by Monaco's `onDidContentSizeChange` event (clamped 120–400px, so it grows with content like the existing `Textarea`'s `field-sizing-content` but caps at 400px and lets Monaco's own scrollbar take over beyond that). Scrollbar styling matched to the app's dark theme via new global `.monaco-editor .scrollbar .slider` rules in `globals.css` (no prior scrollbar theming existed to reuse). Wired into both `ItemDrawer.tsx` (view mode's readonly `<pre>` block and edit mode's `Textarea`) and `NewItemDialog.tsx`'s create form, gated on the existing `LANGUAGE_TYPES` set (`snippet`/`command` only — it already matched the spec's scope exactly, so no new type-check set was needed) — prompts/notes/links keep the plain `Textarea`/`<pre>` untouched. A `readOnly` prop switches between display and edit modes; a small unexported `mapMonacoLanguage()` helper aliases common shorthands (`js`→`javascript`, `sh`→`shell`, etc.) to Monaco's language ids. No new unit tests: every file touched is a `src/components/**` client component (or `globals.css`), out of the project's `src/actions/**`/`src/lib/**`-only Vitest scope — confirmed via `git status` that nothing under `src/actions` or `src/lib` was touched. Verified end-to-end via Playwright against the live dev server: a snippet's Dockerfile content renders with full syntax highlighting and a working copy button (toast fires), a one-line bash command renders at its compact fluid height (not padded to 400px), a prompt item still renders as plain text (confirming the type gating), edit mode is fully typeable with live re-highlighting, and the New Item dialog's language field live-updates both the header label and Monaco's syntax highlighting as it's typed — no console errors throughout. `npm run build`, `npm run lint`, and `npm test` (26 tests, unchanged) all pass.
- **2026-08-16** — Markdown Editor completed on `feature/markdown-editor`. Added `MarkdownEditor.tsx` (`src/components/dashboard/`) using `react-markdown` + `remark-gfm`, styled to match `CodeEditor`'s header (`bg-[#1e1e1e]` container, `bg-[#2d2d2d]` header, copy button) but with a shadcn `Tabs` (newly added via `npx shadcn add tabs`, base-ui-backed) for Write/Preview instead of window dots/language label — readonly mode shows a plain "PREVIEW" label with only the rendered markdown (no tabs), edit mode defaults to the Write tab with Preview available via the tab. Added a `.markdown-preview` CSS class in `globals.css` covering headings (h1–h6), code blocks, inline code, lists, blockquotes, links, and tables for dark-mode GFM rendering. Wired into `ItemDrawer.tsx` (view and edit modes) and `NewItemDialog.tsx`, replacing `Textarea`/`<pre>` for **notes and prompts only**, reusing the already-precise `CONTENT_TYPES`/`LANGUAGE_TYPES` sets from the Code Editor feature (the non-`LANGUAGE_TYPES` branch of `CONTENT_TYPES` is exactly prompt/note, so no new type-check set was needed). Height and scrollbar were tuned via two follow-up passes after initial user feedback: bumped both `CodeEditor` and `MarkdownEditor`'s default min-height from 120px to 200px (still capped at 400px), and added a reusable `.themed-scrollbar` CSS class (thin, `rgba(255,255,255,0.15)` thumb matching Monaco's `.slider` styling, with Firefox `scrollbar-color` support) applied to the Write textarea and Preview panel so native scrollbars match Monaco's. Also widened the item drawer per user feedback — first attempt (`sm:max-w-lg` in `ItemDrawer.tsx`) had no visible effect because `Sheet`'s base component already sets the more-specific `data-[side=right]:sm:max-w-sm`; fixed by matching that same selector pattern (`data-[side=right]:sm:max-w-lg`). Verified end-to-end via Playwright against the live dev server: full GFM rendering (headings, bold, inline code, lists, code blocks, blockquotes, links, tables) in both view and edit-mode Preview, working copy button (toast fires), Write→Preview tab defaults correct in both modes, Snippet's `CodeEditor` confirmed unaffected by the New Item dialog's type switch, the taller default height and Monaco-matching scrollbar both visually confirmed, and the widened drawer visually confirmed only after the selector-specificity fix. `npm run build`, `npm run lint`, and `npm test` (26 tests, unchanged — no server actions/utilities touched) all pass.
- **2026-08-16** — File & Image Upload with Cloudflare R2 completed on `feature/file-image-upload`. Added `src/lib/r2.ts` (an `@aws-sdk/client-s3` client pointed at R2's S3-compatible endpoint, with `uploadToR2`/`deleteFromR2`/`getR2Object`/`r2KeyFromUrl` helpers) and `src/lib/file-constraints.ts` (per-type max size/extension/MIME allowlists plus `formatFileSize`, shared by both server and client code). Added `POST /api/upload` (auth-checked, validates extension/MIME/size server-side, uploads to a `${userId}/${uuid}-${filename}` R2 key) and `GET /api/items/[id]/download` (ownership-checked via the existing `getItemDetail`, streams the R2 object back with a `Content-Disposition: attachment` header — a proxy so the browser never has to deal with R2 CORS directly). Added `FileUpload.tsx` (drag-and-drop + click-to-browse, XHR-based upload with a live progress bar via `xhr.upload.onprogress`, since `fetch` can't report upload progress), wired into `NewItemDialog.tsx` for the `file`/`image` types (also added to `TopBar.tsx`'s `creatableTypeSlugs`, previously limited to the 5 non-file/image types). `src/lib/db/items.ts`'s `CreateItemData`/`createItem` gained `fileUrl`/`fileName`/`fileSize` fields and a `"file"` `contentType`; `deleteItem` now looks up `contentType`/`fileUrl` before deleting and calls `deleteFromR2` afterward so R2 objects don't outlive their item. `ItemDrawer.tsx` shows an `<img>` preview for images vs. a file-icon/name/size block for files, plus a Download button (replacing Copy for `contentType === "file"` items) that links to the download-proxy route — required `nativeButton={false}` on that `Button` since Base UI throws a dev-console error when a `Button` renders as an `<a>` via the `render` prop without it, caught via a Playwright console-error check during testing. A `/feature review` pass caught a real gap before completion: `createItem`'s Zod schema accepted any `fileUrl` string for file/image items, meaning a Server Action call bypassing the UI could skip `/api/upload`'s size/type checks entirely by supplying an arbitrary external URL — fixed with a `.refine()` requiring `fileUrl` to start with `R2_PUBLIC_URL` (skipped only if that env var is unset, matching Vitest's no-`.env`-loading setup). Verified end-to-end via Playwright against the live dev server using real R2 credentials already present in `.env`: uploaded a real PNG (progress bar, then preview) and a real `.txt` file (file-info block), both created items rendering with real `pub-*.r2.dev` URLs; downloaded via the proxy route and confirmed correct `Content-Type`/`Content-Disposition` headers; deleted both items and confirmed their R2 objects independently returned `404` afterward (real cleanup, not just a mocked call); re-verified the security-refix still allows legitimate uploads to succeed. Added 2 tests to `src/actions/items.test.ts` (file-type validation-error and image-type happy-path) alongside updates to the existing create-item tests for the new `fileUrl`/`fileName`/`fileSize` fields. `npm run build`, `npm run lint`, and `npm test` (28 tests, up from 26) all pass.
- **2026-08-16** — Image Gallery View completed on `feature/image-gallery-view`. Added `ImageThumbnailCard.tsx` (`src/components/dashboard/`) replacing `ItemCard` for `image`-type items: 16:9 (`aspect-video`) thumbnail with `object-cover`, a 5%/300ms hover-zoom transition on the image, and a pin/favorite icon overlay — still opens the existing item drawer on click via the same `useItemDrawer` context `ItemCard` uses, so view/edit/delete/favorite/pin all work unchanged. `src/app/(app)/items/[type]/page.tsx` now branches on `itemType.name === "image"`: a fixed 3-column grid of `ImageThumbnailCard`s for images, versus the pre-existing responsive `ItemCard` grid (unchanged) for every other type. `ItemWithType` (`src/lib/db/items.ts`) gained a `fileUrl` field — the raw Prisma row already carried it, it just wasn't surfaced on the lightweight card shape yet — so no new query/select changes were needed in `getItemsByType`/`getPinnedItems`/`getRecentItems`. No new unit tests: everything touched is either a `src/components/**` client component or the `fileUrl` passthrough in a `src/lib/db/**` Prisma wrapper (out of Vitest's `src/actions/**`/`src/lib/**`-excluding-`db`-scope). Verified end-to-end via Playwright against the live dev server using disposable test image items (created/deleted directly via Prisma, since there's no way to create `image`-type items through the create-item UI yet — only file/image *upload* exists): the 3-column gallery renders correctly with pin/favorite overlays, clicking a thumbnail opens the item drawer exactly like `ItemCard` did, and `/items/snippets` confirmed unaffected (still the original responsive grid). `npm run build`, `npm run lint`, and `npm test` (28 tests, unchanged) all pass.
- **2026-08-16** — File List View completed on `feature/file-list-view`. Added `FileListRow.tsx` (`src/components/dashboard/`) replacing `ItemCard` for `file`-type items on `/items/files`: a single-column list of rows (Google Drive/Dropbox style), each showing an extension-derived icon (PDF/TXT→`FileText`, MD/YAML/XML→`FileCode`, JSON→`FileJson`, CSV→`FileSpreadsheet`, TOML/INI→`FileCog`, unknown→`File`, resolved via a module-level `EXTENSION_ICONS` map plus a `default` fallback key so the render body stays a single member lookup — an earlier version computed the icon through a small helper function/logical-OR fallback and tripped the `react-hooks/static-components` lint rule, which only accepts a plain indexing expression, matching the pattern already used by `itemTypeIcons[...]` elsewhere in the codebase), file name, size, relative upload date, pin/favorite indicators, and a Download button (same `nativeButton={false}` + `render={<a href=".../download">}` pattern as the item drawer's download button, from `feature/file-image-upload`) with `stopPropagation` so it triggers a direct download instead of opening the row's item drawer. `src/app/(app)/items/[type]/page.tsx` now branches on `itemType.name === "file"` alongside the existing `"image"` gallery branch; images and all other types are unaffected. `ItemWithType` (`src/lib/db/items.ts`) gained `createdAt`/`fileName`/`fileSize` fields — the raw Prisma rows already carried them unselected, so no query changes were needed, only extending the interface and its `toItemWithType` mapper. No new unit tests: everything touched is a `src/components/**` client component or a passthrough-field addition to a `src/lib/db/**` Prisma wrapper, both out of Vitest's `src/actions/**`/`src/lib/**`-excluding-`db` scope. Verified end-to-end via Playwright against the live dev server using disposable test file items (created/deleted directly via Prisma): rows render with correctly-differentiated per-extension icons (confirmed via each row's actual SVG class, since they look visually similar at that size/color), hover highlight, clicking a row opens the `ItemDrawer`, clicking Download does not open the drawer (`stopPropagation` confirmed — it navigated to the download route instead, 500ing only because the disposable test items used a fake, non-existent R2 URL), and the layout stacks info vertically under a 390px mobile viewport. `npm run build` and `npm run lint` both pass.
- **2026-08-16** — Code-Scanner Drawer Cleanup completed on `fix/code-scanner-drawer-cleanup`. `ItemDrawer.tsx` had grown across four prior features (drawer view, edit mode, delete, file/image support) into one oversized component; split it into `ItemDrawerView.tsx` (read-mode content), `ItemDrawerEditForm.tsx` (edit-mode fields), `ItemDrawerActions.tsx` (Favorite/Pin/Copy/Download/Edit/Delete action bar), `use-item-drawer-data.ts` (fetch/loading-state hook), `item-content-types.ts` (shared `CONTENT_TYPES`/`LANGUAGE_TYPES` sets), and `SectionLabel.tsx` (small shared heading), leaving `ItemDrawer.tsx` itself as a thin composition shell. `ItemCard`, `FileListRow`, and `ImageThumbnailCard` had each independently duplicated the same open-drawer `onClick`/`onKeyDown` handler pair since the Image Gallery/File List View features; extracted into a shared `use-drawer-card-props.ts` hook (spread onto each card's root element) instead. Replaced the two remaining raw `<img>` usages (`FileUpload.tsx`'s upload preview, `ImageThumbnailCard.tsx`'s thumbnail) with `next/image`, per a coding-standards gap the prior file/image features had left as an explicit `eslint-disable` — added the R2 public bucket hostname to `next.config.ts`'s `remotePatterns` to make this possible, and codified the rule ("Always use next/image. Never suppress `@next/next/no-img-element`") in `coding-standards.md` so it doesn't regress. Also closed two smaller gaps found in the same pass: added a 20/hour-per-user rate limiter (`rate-limit.ts`'s `rateLimiters.upload`, wired into `POST /api/upload`) since uploads had none of the six other auth/mutation endpoints' rate limiting, and dropped `.svg`/`image/svg+xml` from `file-constraints.ts`'s allowed image types (SVGs can carry embedded script and DevStash serves uploaded files back from R2 with no sanitization). Updated `docs/item-crud-architecture.md` and `docs/item-types.md` with "superseded"/"partially outdated" status notes and inline corrections now that the design docs they were written against (pre-CRUD, pre-upload) are stale, and added a permanent reminder to both `code-scanner.md` (grep for `eslint-disable` and check suppressions are still justified) and `ai-interaction.md` (run a `code-scanner` pass for oversized files/duplicated logic as part of periodic code review). `npm run build`, `npm run lint`, and `npm test` (28 tests, unchanged) all pass. Merged to `main`, branch deleted.
- **2026-08-16** — Collection Create completed on `feature/collection-create`. Added a "New Collection" `Dialog` (`NewCollectionDialog.tsx`) wired to `TopBar.tsx`'s existing (previously inert) button, with name (required)/description (optional) fields — collections have no `itemType`/content, so the form is simpler than `NewItemDialog`. Followed the item-create pattern exactly: `createCollection(input)` in a new `src/actions/collections.ts` (Zod-validated, `{ success, data, error }`, session via `auth()`) delegating to a new `createCollection(userId, data)` query fn in `src/lib/db/collections.ts`; no new API route, consistent with this project's Server-Action-by-default preference. On success: sonner toast, dialog closes/resets, `router.refresh()` updates the dashboard's stats row/"Recent Collections" grid and the sidebar's collection list/count live (all already DB-backed from earlier features, so no changes needed there). Added `src/actions/collections.test.ts` (4 tests: no-session, empty-name validation, trimmed-name/null-description happy path, description-provided happy path), mirroring `items.test.ts`'s pattern; the new `lib/db/collections.ts` query fn stays untested per the established `src/lib/db/**` exclusion. Verified end-to-end via Playwright against the live dev server: created a real collection (toast fired, dialog closed, stats row went 5→6, new card appeared live in "Recent Collections" with correct name/description/0 items, sidebar unaffected since new collections aren't favorited by default), then deleted the disposable test collection directly via Prisma and confirmed the dashboard reverted to baseline (5 collections, no stray card) with no console errors throughout. `npm run build`, `npm run lint`, and `npm test` (32 tests, up from 28) all pass.
- **2026-08-16** — Add Item to Collections completed on `feature/item-add-to-collections`. Added a multi-select "Collections" input to both the New Item dialog and the Item Drawer's edit mode, letting an item be added to one or more collections at once (many-to-many, via the existing `ItemCollection` join table) — collection detail/listing pages deliberately stayed out of scope. New reusable `CollectionSelect.tsx` (`src/components/dashboard/`) built on shadcn's existing `DropdownMenu`/`DropdownMenuCheckboxItem` (`closeOnClick={false}` so multiple toggles don't dismiss the menu) rather than adding a new UI-kit component, since a checkbox-item dropdown was already available. `createItem`/`updateItem` (`src/actions/items.ts`) gained a Zod-validated `collectionIds: z.array(z.string())` field, threaded through to matching `CreateItemData`/`UpdateItemData` in `src/lib/db/items.ts`; both query fns now write to `ItemCollection` via a new `ownedCollectionIds(userId, collectionIds)` helper that filters the submitted ids down to ones the user actually owns before persisting — closes off a Server-Action-bypass path where arbitrary collection ids could otherwise be attached to another user's collection. Added `getUserCollections(userId)` to `src/lib/db/collections.ts` (lightweight `{id, name}` list for the selector, distinct from the existing stats-bearing `getCollectionsWithStats`); `(app)/layout.tsx` fetches it once and passes it down through `TopBar`→`NewItemDialog` and directly to `ItemDrawer`→`ItemDrawerEditForm`. Updated `src/actions/items.test.ts` fixtures/assertions for the new required field (all 32 tests still pass, no new tests needed since `collectionIds` follows the same pass-through pattern already covered). Verified end-to-end via Playwright against the live dev server: created a real item with 2 collections selected (sidebar/dashboard counts updated live), confirmed the drawer's view-mode Collections section rendered them, edited the item to swap one collection for another, saved, and confirmed both the view mode and sidebar counts reflected the change correctly. Hit the same recurring stray-Chrome-process issue as a prior feature (`feature/item-create`'s history entry) — killed the leftover `mcp-chrome-*` profile process before Playwright could attach — and cleaned up the disposable test item directly via Prisma (a temporary script, deleted afterward) after the drawer's Delete confirmation button became unclickable behind a lingering success toast; confirmed the dashboard returned to baseline (20 items, 6 collections) afterward. `npm run build`, `npm run lint`, and `npm test` (32 tests, unchanged) all pass.
- **2026-08-16** — Collections Pages completed on `feature/collections-pages`. Added `/collections` (lists all of a user's collections via the existing `CollectionCard`) and `/collections/[id]` (a single collection's items), closing the gap `feature/item-add-to-collections` deliberately left open. Added `getCollectionDetail(userId, collectionId)` and `getAllCollections(userId)` to `src/lib/db/collections.ts`, both following the same ownership-scoping (`findFirst({ id, userId })`) and `CollectionTypeSummary` aggregation patterns already used by `getCollectionsWithStats`/`getItemDetail`; `ItemWithType` (`src/lib/db/items.ts`) gained an `itemType.name` field so the detail page can branch per-item between `FileListRow` (files), `ImageThumbnailCard` (images), and `ItemCard` (everything else) — same branching `/items/[type]` already does per-route, just per-item here since a collection can mix types. `CollectionCard` is now wrapped in a `next/link` to its `/collections/[id]` page (covers both the dashboard's "Recent Collections" grid and the new listing page for free, since both already render it); the sidebar's "View all collections" link already pointed at `/collections` from `feature/stats-sidebar`, so no change was needed there. Two follow-up rounds after initial completion, both user-requested: (1) split the detail page's Images/Files into their own labeled sections when non-empty, and added a type icon+count row below the description (`CollectionDetail` gained a `types: CollectionTypeSummary[]` field, computed identically to `getCollectionsWithStats`'s per-collection type aggregation); (2) reordered those sections so other items render first, then Images, then Files. No new unit tests: every file touched is either `src/lib/db/**` (Prisma query wrappers, explicitly out of scope per `ai-interaction.md`) or a `src/app/**`/`src/components/**` page/component (also out of Vitest's scope) — confirmed the existing 32 tests still pass unchanged. Verified end-to-end via Playwright against the live dev server: `/collections` renders all 6 real collections with correct links, a command-only collection renders via `ItemCard` with a working item drawer, and disposable test images/files (created directly via Prisma, cleaned up afterward) confirmed the IMAGES/FILES sections, the type-count row, and the reordered section layout all render correctly. `npm run build`, `npm run lint`, and `npm test` (32 tests, unchanged) all pass.
- **2026-08-16** — Collection Card Actions (Edit, Delete, Favorite icon) completed on `feature/collection-card-actions`. Added Edit/Delete/Favorite to the collection detail page (`CollectionDetailActions.tsx`, an action bar next to the `<h1>`) and a 3-dots dropdown to `CollectionCard` (`CollectionCardMenu.tsx`, shared by `/collections` and the dashboard's "Recent Collections" grid) with the same three entries. Favorite is icon-only for now (no click handler, per spec — favoriting logic is a future feature). Edit opens a shared `EditCollectionDialog.tsx` (name/description form, mirroring `NewCollectionDialog`'s conventions) backed by a new `updateCollection` Server Action (`src/actions/collections.ts`) + query fn (`src/lib/db/collections.ts`, ownership-checked via `findFirst`); resets its form on close via the `Dialog`'s `onOpenChange` callback rather than a `useEffect`, since the codebase's set-state-in-effect lint rule (already worked around once in the item-drawer feature) rejected the first draft. Delete opens a shared `DeleteCollectionDialog.tsx` (plain `AlertDialog` confirm/cancel, not the typed-`DELETE` pattern used by account deletion) backed by a new `deleteCollection` Server Action + query fn — deleting only removes the `Collection` row; the schema's existing `onDelete: Cascade` on `ItemCollection.collection` cleans up the join rows, and items themselves are never touched (confirmed live: deleted a collection containing a "Test command" item, the item still appeared under `/items/commands` afterward). Discovered mid-implementation that `CollectionCard`'s original `next/link` wrapper was unsafe once a portal-rendered `Dialog`/`AlertDialog` needed to nest inside it: React re-parents portaled content into the React (not DOM) tree for event bubbling, so any click inside the Edit/Delete dialogs — not just the 3-dots trigger — would bubble up and fire the Link's own navigation handler (confirmed via `next/dist/client/link.js`'s `e.defaultPrevented` check). Fixed by converting `CollectionCard` from an anchor to a `role="link"` `div` with `onClick`/`onKeyDown`-driven `router.push` (matching the existing `useDrawerCardProps` convention elsewhere in the app), with `CollectionCardMenu` wrapped in a `stopPropagation` boundary — a plain-handler stop reliably blocks the parent's `onClick`/`onKeyDown` regardless of what's clicked inside, unlike the anchor-based approach. Trade-off: `CollectionCard` no longer supports native middle-click/open-in-new-tab, accepted as a reasonable cost for correctness. Added `updateCollection`/`deleteCollection` tests to `src/actions/collections.test.ts` (7 new tests: no-session, validation, not-found, and happy-path for each), mirroring the existing `createCollection` suite. Verified end-to-end via Playwright against the live dev server (after clearing the recurring stray `mcp-chrome-*` profile processes blocking the browser session): dropdown opens without navigating on both `/collections` and the dashboard, Edit prefills and saves correctly (survives a page reload), Delete's confirmation copy and behavior are correct, and clicking the card body elsewhere still navigates to the detail page. `npm run build`, `npm run lint`, and `npm test` (39 tests, up from 32) all pass.
- **2026-08-16** — Global Search / Command Palette completed on `feature/global-search`. Added a Cmd+K / Ctrl+K command palette (shadcn `cmdk`, newly installed) grouping fuzzy-searched results into Items and Collections sections; selecting an item opens the existing `ItemDrawer`, selecting a collection navigates to `/collections/[id]`. Data is pre-fetched server-side once in `(app)/layout.tsx` (a new `getAllItemsForSearch(userId)` in `src/lib/db/items.ts`, plus the existing `getAllCollections`) and filtered entirely client-side — no server round-trips per keystroke. New `command-palette-context.tsx` (open/setOpen, mirroring the existing `item-drawer-context.tsx`/`sidebar-context.tsx` pattern) is shared between `CommandPalette.tsx` (global keydown listener for the shortcut) and `TopBar.tsx`, whose search input is now a read-only, click-to-open field with an outlined `⌘K` badge anchored to its right edge instead of inline placeholder text. Hit and fixed a real bug in shadcn's generated `command.tsx`: `CommandDialog` didn't wrap its children in the `Command` primitive, crashing on open with `Cannot read properties of undefined (reading 'subscribe')` — fixed in `CommandPalette.tsx` by wrapping its content in `<Command>` rather than modifying the generated UI file. Two rounds of user feedback on match quality after initial completion: first, cmdk's default `command-score` filter matched loose out-of-order character subsequences (e.g. "docker" matching "useDebounce hook"), swapped for a strict substring filter — then tightened further per follow-up feedback that removed all typo tolerance, landing on cmdk's own `defaultFilter` (`command-score`, re-exported from the `cmdk` package) gated by a `MIN_MATCH_SCORE = 0.05` threshold: verified via a standalone Node script that this keeps single-typo/transposition queries (`dcoker`, `dockr`, `reactpaterns`) matching correctly while still scoring truly unrelated text near zero. No new unit tests: every file touched is a `src/components/**`/`src/app/**` component or a lightweight passthrough addition to a `src/lib/db/**` Prisma wrapper, both out of Vitest's `src/actions/**`/`src/lib/**`-excluding-`db` scope — confirmed the existing 39 tests still pass unchanged. Verified end-to-end via Playwright against the live dev server: both Ctrl+K and the TopBar click open the palette, fuzzy/typo-tolerant search narrows results live, Enter/click selection opens the item drawer or navigates to a collection correctly, and no console errors throughout. `npm run build`, `npm run lint`, and `npm test` (39 tests, unchanged) all pass.
- **2026-08-17** — Pagination completed on `feature/pagination`. Added numbered pagination with disabled prev/next at the boundaries to `/items/[type]`, `/collections`, and `/collections/[id]`, all backed by real DB-level `skip`/`take` Prisma queries instead of fetching entire result sets: `getItemsByType` and `getCollectionDetail` (`src/lib/db/items.ts`/`collections.ts`) now return `{ items, totalCount }`, and a new `getCollectionsPage` paginates the `/collections` listing itself. `getCollectionDetail`'s per-collection type-count summary (icons/counts shown above the item grid) still reflects *all* items in the collection, not just the current page, via a separate lightweight query selecting only `itemType` fields (no item content/tags), so pagination doesn't skew those counts. Added a new `src/lib/constants.ts` (`ITEMS_PER_PAGE`/`COLLECTIONS_PER_PAGE = 21`, `DASHBOARD_COLLECTIONS_LIMIT = 6`, `DASHBOARD_RECENT_ITEMS_LIMIT = 10`) replacing magic numbers on the dashboard's recent-collections/recent-items limits. Installed shadcn's `pagination` component and built a reusable `PaginationControls.tsx` on top of it (windowed page numbers with ellipsis, greyed-out/non-interactive prev/next at either end). Fixed a real bug surfaced by testing: the shadcn-generated `pagination.tsx` set a `data-slot="pagination-link"` on its `<a>` that conflicted with `Button`'s own hardcoded `data-slot="button"` (Base UI's `render`-prop merge resolves this differently between SSR and hydration), causing a React hydration-mismatch console error on every load — fixed by dropping the redundant attribute, since nothing in the codebase selected on it. `COLLECTIONS_PER_PAGE` governs the `/collections` listing (paginating collections themselves); `/collections/[id]` paginates the *items* inside one collection and uses `ITEMS_PER_PAGE` instead — initially wired backwards (both `/collections/[id]` on `COLLECTIONS_PER_PAGE` and no pagination at all on `/collections`), caught and fixed after user testing. `getCollectionsPage` paginates by slicing the existing cached `getCollectionsWithStats(userId)` result rather than a true DB `skip`/`take`, since collection counts are small (free tier caps at 3) and a real paginated query would mean forking the shared cache the sidebar/dashboard also depend on; the item-heavy `/items/[type]` and `/collections/[id]` queries do use real DB-level pagination. Verified end-to-end via Playwright against the live dev server using disposable test data (20 temporary command items, created and cleaned up via a temporary `tsx` script matching `scripts/delete-non-demo-users.ts`'s Neon-adapter pattern): multi-page navigation and correct prev/next disabling on `/items/[type]`, `/collections`, and `/collections/[id]`, and the collection type-count row staying correct (summed across all items) even when only 2 items per page were shown — no console errors on any of the three pages. `npm run build`, `npm run lint`, and `npm test` (39 tests, unchanged — no server actions/utilities touched) all pass.
- **2026-08-17** — Settings Page completed on `feature/settings-page`. Added a protected `/settings` route under the existing `(app)` route group (added to `src/proxy.ts`'s matcher alongside `/dashboard`/`/profile`) and a "Settings" entry in the sidebar user dropdown (`SidebarContent.tsx`), above the existing "Sign out" item, using Base UI's `render` prop (`DropdownMenuItem render={<Link href="/settings" />}`) rather than a Radix-style `asChild`. Moved the profile page's "Account actions" card (`ChangePasswordDialog` + `DeleteAccountDialog`) from `/profile` to the new `/settings` page — `/profile` now shows only account info + usage stats. Confirmed with the user during spec review that "forgot password" in the request meant the existing `ChangePasswordDialog` (change password while logged in), not the separate `/forgot-password` email-reset flow — no new forgot-password link was added. Also relocated `ChangePasswordDialog.tsx`/`DeleteAccountDialog.tsx` from `src/components/profile/` to `src/components/settings/` per `coding-standards.md`'s `[feature]`-scoped file convention, since they're no longer profile-owned. No new unit tests: every file touched is a `src/app/**`/`src/components/**` page/component or the `src/proxy.ts` matcher, all out of Vitest's `src/actions/**`/`src/lib/**`-excluding-`db` scope — confirmed the existing 39 tests still pass unchanged. Verified end-to-end via Playwright against the live dev server (after clearing the recurring stray `mcp-chrome-*` profile processes blocking the browser session): dropdown's Settings link navigates to `/settings` and renders the moved Account actions card, `/profile` no longer shows it, an unauthenticated request to `/settings` redirects to `/sign-in?callbackUrl=%2Fsettings`, and signing in from there correctly lands back on `/settings`; no console errors throughout. `npm run build`, `npm run lint`, and `npm test` (39 tests, unchanged) all pass.
- **2026-08-17** — Editor Preferences Settings completed on `feature/editor-preferences-settings`. Added an "Editor preferences" card to `/settings` (`EditorPreferencesSettings.tsx`): font size and tab size dropdowns (shadcn `select`, newly installed), word wrap and minimap toggles (shadcn `switch`, newly installed, defaults on/off respectively), and a theme dropdown (vs-dark/monokai/github-dark, default vs-dark) with a live Monaco preview panel. Since only `vs-dark` ships built into Monaco, monokai/github-dark are hand-defined color themes (`src/lib/monaco-themes.ts`, `monaco.editor.defineTheme`) registered via `CodeEditor.tsx`'s `beforeMount` hook. Added an `editorPreferences` `Json?` column on `User` via a real migration (`prisma migrate dev`, `20260817122908_add_editor_preferences`), with `getEditorPreferences`/`updateEditorPreferences` in `src/lib/db/user.ts` (a `parseEditorPreferences` helper merges stored JSON against `DEFAULT_EDITOR_PREFERENCES` field-by-field so a partial/corrupt value never crashes rendering) and a Zod-validated `updateEditorPreferences` Server Action (`src/actions/editor-preferences.ts`, `{success, data, error}` pattern). A new client-only `EditorPreferencesContext`/`EditorPreferencesProvider` (`src/components/dashboard/editor-preferences-context.tsx`) holds the preferences in state, seeded from a server-fetched value in `(app)/layout.tsx`, and auto-saves on every change (no save button) via the Server Action, showing a sonner success/error toast — `CodeEditor.tsx` now reads `fontSize`/`tabSize`/`wordWrap`/`minimap`/`theme` from this context via `useEditorPreferences()` instead of hardcoded Monaco options, so the same preferences apply everywhere `CodeEditor` is used (item drawer, new-item dialog, settings preview). Hit and fixed a real gotcha mid-implementation: the already-running dev server (started before this feature's migration) kept using its pre-migration generated Prisma client and threw `Unknown field editorPreferences` until restarted — `npx prisma generate` alone wasn't enough since the dev server's own module cache was stale. Also hit a build-time Prisma typing issue (a plain `EditorPreferences` object isn't structurally assignable to Prisma's `InputJsonValue` without an index signature) fixed with an explicit cast. Added `src/actions/editor-preferences.test.ts` (5 tests: no-session, invalid font size/tab size/theme validation, happy-path delegation), following the existing `collections.test.ts` pattern; `src/lib/db/user.ts`'s new functions stay untested per the established `src/lib/db/**` exclusion. Verified end-to-end via Playwright against the live dev server: theme/toggle changes apply live to the settings page's Monaco preview (confirmed via screenshot for monokai's syntax colors and the minimap appearing), a font-size change persisted correctly across a full page reload (double-checked via a direct, disposable DB read script), and the demo user's preferences were reset back to defaults afterward. Noticed unrelated preference values flapping mid-test, traced to another browser session sharing the same signed-in demo-user cookie concurrently hitting the same dev server during the test window (visible in server logs as `/items/snippets` and `/api/items/*` requests never issued by this session) — not a bug in this feature, and the final persisted state was clean, but worth flagging as an environment quirk. `npm run build`, `npm run lint`, and `npm test` (44 tests, up from 39) all pass.
- **2026-08-17** — Favorites Page completed on `feature/favorites-page`. Added a protected `/favorites` route listing a user's favorited items and collections in a compact, monospace, VS Code/terminal-style list (icon, title, type badge, relative date), linked from a new star button in `TopBar.tsx` (`src/proxy.ts`'s matcher extended alongside `/dashboard`/`/profile`/`/settings`). Added `getFavoriteItems(userId)` to `src/lib/db/items.ts` (isFavorite filter, sorted by `updatedAt` desc); reused the existing `getFavoriteCollections` from `src/lib/db/collections.ts` as-is, already sorted the same way. New `FavoriteItemRow.tsx`/`FavoriteCollectionRow.tsx` (`src/components/dashboard/`) reuse the established `useDrawerCardProps` hook (items → `ItemDrawer`) and the `role="link"`/`router.push` pattern from `CollectionCard` (collections → `/collections/[id]`). Two rounds of user-requested styling follow-ups after initial completion: (1) type badges colored via inline `style` using each item's/collection's own `itemType.color`, matching the color-coding convention used elsewhere (`ItemCard`, `CollectionCard`); (2) moved the favorited-indicator star to sit once beside the page's "Favorites" heading instead of repeating on every row (redundant on an already-favorites-only page). No new unit tests: `getFavoriteItems` is a `src/lib/db/**` Prisma query wrapper, out of Vitest's scope per the same exclusion as its sibling `getFavoriteCollections`; every other file touched is a `src/app/**`/`src/components/**` page/component. Verified end-to-end via Playwright against the live dev server using real seeded favorites (7 items, 2 collections): correct counts/sections/empty-state logic, per-type badge colors, clicking an item opens the drawer with full details, clicking a collection navigates to its detail page, no console errors throughout. `npm run build`, `npm run lint`, and `npm test` (44 tests, unchanged — no server actions/utilities touched) all pass.
- **2026-08-17** — Favorite Toggle (Drawer, Collection Page, Cards) completed on `feature/favorite-toggle`. Wired up the previously inert/display-only favorite stars into real toggles across four surfaces: the Item Drawer's Favorite button, the collection detail page's Favorite button, the collection card's 3-dots menu Favorite item, and the star indicator directly on `ItemCard`/`CollectionCard` (clickable in place, `stopPropagation`'d so it never opens the drawer or navigates). Added `toggleItemFavorite(itemId)`/`toggleCollectionFavorite(collectionId)` Server Actions (`src/actions/items.ts`/`collections.ts`, ownership-checked, `{success, isFavorite, error}` pattern) delegating to matching `src/lib/db` query fns. Two rounds of user-reported bugs fixed after initial completion: (1) `CollectionCardMenu`'s dropdown label/star and `CollectionCard`'s own star were out of sync because each held independent local `isFavorite` state — fixed by removing local state from `ItemCard`/`CollectionCard` entirely (render directly from props, which `router.refresh()` correctly re-delivers) and lifting the toggle handler from `CollectionCardMenu` up into `CollectionCard` so both share one source of truth; (2) toggling favorite was reordering "Recent Items"/collection listings because `prisma.item.update()`/`prisma.collection.update()` auto-bump `@updatedAt` on every call — fixed by rewriting both `toggleItemFavorite`/`toggleCollectionFavorite` (`src/lib/db/items.ts`/`collections.ts`) as raw `$queryRaw` UPDATE statements, which bypass Prisma Client's mutation resolver and leave `updatedAt` untouched, confirmed via Playwright that a toggled item's position/timestamp no longer changes. Also found and fixed unrelated demo-data drift (the `Design Resources` collection had been left unfavorited by an earlier batch operation/reset, unrelated to this feature's code) via a disposable raw-SQL script, restoring it to the seeded baseline. Added 6 new Vitest tests (`toggleItemFavorite`/`toggleCollectionFavorite`, no-session/not-found/happy-path each) to `src/actions/items.test.ts`/`collections.test.ts`, mirroring the existing update/delete suites; the `src/lib/db/**` query fns stay untested per the established exclusion. Verified end-to-end via Playwright against the live dev server across all four toggle points plus the two bug fixes, with favorite/stat counts and sidebar groupings confirmed correct throughout and no console errors. `npm run build`, `npm run lint`, and `npm test` (50 tests, up from 44) all pass.
- **2026-08-17** — Favorites Page Sorting completed on `feature/favorites-page-sorting`. Added a client-side sort control to `/favorites` with 5 options — Newest first (default), Oldest first, A to Z, Z to A, Item type — via a new `FavoritesList.tsx` client component wrapping the existing `FavoriteItemRow`/`FavoriteCollectionRow` lists; `favorites/page.tsx` still fetches `getFavoriteItems`/`getFavoriteCollections` server-side as before and just passes the results down, so no new server actions or DB queries were needed. Items sort by `title`/`updatedAt`/`itemType.name`; since collections have no single type, "Item type" falls back to A-Z for the Collections section. Built on shadcn's existing `Select` (Base UI-backed). Hit and fixed a real Base UI gotcha: `Select.Value` renders the raw selected value by default (e.g. the trigger showed literal `az`/`newest` instead of "A to Z"/"Newest first") — Base UI's `Select.Value` only auto-derives a label when given a render-function `children` prop (`(value) => ReactNode`), so `SelectValue` needed `{(value: SortOption) => SORT_LABELS[value]}` explicitly rather than relying on automatic label lookup from `SelectItem`'s content. No new unit tests: everything touched is a `src/components/**` client component or a `src/app/**` page, both out of Vitest's `src/actions/**`/`src/lib/**`-excluding-`db` scope. Verified end-to-end via Playwright against the live dev server: all 5 sort orders render correctly for both Items and Collections sections (including the Item-type fallback and the closed-select label fix), no console errors throughout. `npm run build`, `npm run lint`, and `npm test` (50 tests, unchanged — no server actions/utilities touched) all pass.
- **2026-08-17** — Pinned Items completed on `feature/pinned-items`. Made the existing (previously inert) Pin button in `ItemDrawer` functional, following `feature/favorite-toggle`'s pattern closely: added `toggleItemPin(itemId)` in `src/actions/items.ts` (session via `auth()`, `{success, isPinned, error}` pattern) delegating to a matching `toggleItemPin(userId, itemId)` in `src/lib/db/items.ts`, ownership-checked via `findFirst` and written as a raw `$queryRaw` UPDATE (same as `toggleItemFavorite`) so pinning doesn't bump `updatedAt` and reorder recency-sorted lists. Unlike Favorite's toggle handler, `ItemDrawer.tsx`'s `handleTogglePin` is genuinely optimistic (flips `isPinned` in state before the server call resolves, reverting on failure) and shows a sonner success/error toast, both explicitly required by the spec. Pinned items now sort to the top of the two real item-listing grids — `getItemsByType` (`/items/[type]`) and `getCollectionDetail` (`/collections/[id]`) — via a compound `orderBy: [{isPinned: "desc"}, {updatedAt: "desc"}]`; the dashboard's existing "Pinned Items" section (`getPinnedItems`, from `feature/dashboard-items`) and `ItemCard`'s pin icon needed no changes, already covering the spec's other two pin-related requirements. Added 3 tests to `src/actions/items.test.ts` (no-session, item-not-found, happy-path delegation), mirroring the existing `toggleItemFavorite` suite; the `src/lib/db/**` query fn stays untested per the established exclusion. Verified end-to-end via Playwright against the live dev server: pinning "Docker full prune" from its drawer showed the "Item pinned" toast and the `bg-secondary`/`fill-current` pinned button styling, moved it to the top of `/items/commands`, then unpinning it restored the original order and showed "Item unpinned" — no console errors throughout. `npm run build`, `npm run lint`, and `npm test` (53 tests, up from 50) all pass.
- **2026-08-17** — Homepage Marketing Mockup completed on `feature/homepage-mockup`. Built a standalone static HTML/CSS/JS prototype (`prototypes/homepage/index.html`, `styles.css`, `script.js`, not integrated into the Next.js app) covering the full marketing homepage: fixed top nav (opacity change on scroll), gradient hero headline with a "chaos to order" hero visual (8 floating brand/tool icons that drift, bounce off walls, and repel from the mouse cursor via a `requestAnimationFrame` loop, a pulsing transform arrow, and a dashboard preview mockup), a 6-card features grid tinted per item-type accent color, an AI section (Pro badge + capability checklist + code-editor/AI-tags mockup), a pricing section (Free vs Pro with a monthly/yearly toggle), a CTA section, and a footer with a live copyright year. Scroll-triggered fade-in animations via `IntersectionObserver`. Responsive: hero visual stacks vertically on mobile with the arrow rotated 90°. Reference-only output, not wired into the app — informed the subsequent real-implementation spec (`context/features/homepage-spec.md`).
- **2026-08-17** — Homepage (Real Implementation) completed on `feature/homepage-implementation`. Replaced the placeholder `src/app/page.tsx` with the real marketing homepage, porting `prototypes/homepage/` into Tailwind v4 + shadcn/ui components under `src/components/homepage/`: `Navbar`, `Hero` (chaos → arrow → dashboard preview), `Features`, `AISection`, `Pricing`, `CTA`, `Footer`. Client components limited to `ChaosField` (the mouse-repelling `requestAnimationFrame` icon animation, ported near-directly from `script.js` using refs instead of DOM queries), `ScrollFadeIn` (`IntersectionObserver`-driven fade-in wrapper), `Navbar` (scroll listener for opacity), and `PricingToggle` — which owns both pricing cards in one client boundary (not just the switch) so the Monthly/Yearly toggle can drive the Pro card's price/note without needing cross-component state sharing. Real app item-type accent colors hardcoded in a new `src/lib/homepage-data.ts` (feature cards, plan features, footer columns as data arrays, per the DRY requirement) rather than the mockup's placeholder palette. lucide-react icons throughout except the four brand logos (Notion, GitHub, Slack, VS Code) in the chaos animation, kept as inline SVGs (`src/components/homepage/BrandIcons.tsx`) per spec. Added a `pulse-arrow` CSS keyframe to `globals.css` for the transform arrow — had to split it onto a wrapping element separate from the responsive `rotate-90 lg:rotate-0` utility classes, since a CSS animation's own `transform` in its keyframes would otherwise silently override a static `rotate` utility on the same element. Set the root layout's placeholder `create-next-app` metadata title/description to "DevStash". Two rounds of follow-up polish after initial completion: removed the pricing section's `bg-muted/30` background; changed all "Get Started Free" button copy to "Get Started"; colored the `Layers` logo icon `text-blue-500` (matching the homepage's own nav/footer) on `/sign-in`, `/register`, `/forgot-password`, and `/reset-password` — previously plain/uncolored on all four. No new unit tests: `homepage-data.ts` holds only data constants, no functions worth testing; every other file touched is a `src/app/**`/`src/components/**` page/component, out of Vitest's `src/actions/**`/`src/lib/**`-excluding-`db` scope. Verified end-to-end via Playwright against the live dev server at both desktop (1440px) and mobile (375px) widths: chaos icons confirmed genuinely animating (position/rotation/scale sampled twice, changing over time), transform arrow rotates 90° and hero visual stacks vertically on mobile, dashboard mock renders all 7 item types with correct colors, features grid collapses to 1 column on mobile, pricing toggle correctly swaps $8/mo ↔ $72/yr with the "billed annually" note, no console errors throughout. Also killed a recurring stray `mcp-chrome-*` profile Chrome process blocking the Playwright session, same recurring issue noted in several prior features' history entries. `npm run build`, `npm run lint`, and `npm test` (53 tests, unchanged — no server actions/utilities touched) all pass.
